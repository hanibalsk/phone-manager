# API Configuration
# The base URL for the backend API server
# - For local development: http://localhost:8080
# - For same-origin deployment (static export served by Rust): leave empty or omit
# - For cross-origin: full URL like https://api.example.com
NEXT_PUBLIC_API_URL=http://localhost:8080

# Authentication Mode
# - localStorage: Tokens stored in browser localStorage (default, for development)
# - httpOnly: Tokens stored in httpOnly cookies (recommended for production)
# Note: httpOnly mode requires backend support for cookie-based auth
# See: docs/backend-httponly-auth-requirements.md
NEXT_PUBLIC_AUTH_MODE=localStorage

# App Configuration
NEXT_PUBLIC_APP_NAME=Phone Manager Admin
