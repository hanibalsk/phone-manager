# Create Geofence Flow
# Tests basic navigation after login

appId: three.two.bit.phonemanager.dev
name: Create Geofence Flow

---
# Login first
- runFlow: "../../shared/login_steps.yaml"

# Verify we're on home screen after login
- assertVisible:
    id: "home_screen"

# Take screenshot of home screen
- takeScreenshot: "geofence_home"

# Navigate to settings
- tapOn:
    id: "settings_button"

# Wait for settings screen
- extendedWaitUntil:
    visible:
      id: "settings_screen"
    timeout: 5000

# Take screenshot of settings
- takeScreenshot: "geofence_settings"
